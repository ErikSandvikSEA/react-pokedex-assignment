//import actions
import {} from './actions'

//initialState
const initialState = {}

export const reducer = (state = initialState, action) => {
    switch(action.type) {
         default:
              return state
    }
}